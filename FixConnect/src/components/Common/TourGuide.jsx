// React Joyride has peer dependency issues with React 19 and ESM module resolution in Vite 8.
// We are temporarily disabling it until react-joyride fully supports React 19.

import React from 'react';

const TourGuide = () => {
  return null;
};

export default TourGuide;
